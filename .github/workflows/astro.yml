name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

env:
  BUILD_PATH: "repo"  # the repo is checked out here

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: repo  # clones repo into /home/runner/work/space-for-neuro/repo

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: ${{ env.BUILD_PATH }}
        run: npm ci

      - name: Build Astro site
        working-directory: ${{ env.BUILD_PATH }}
        run: npm run build

      - name: Verify build output
        run: ls -al ${{ env.BUILD_PATH }}/dist

      - name: Add .nojekyll
        run: touch ${{ env.BUILD_PATH }}/dist/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ env.BUILD_PATH }}/dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4